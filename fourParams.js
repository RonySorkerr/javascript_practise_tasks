// Take four parameters
// Multiply the four numbers
// Return the result

function multiply(a, b, c,d){
    const sum = a * b * c* d;
    return sum;
}

const values = multiply(4,5,6,9);
console.log(values);